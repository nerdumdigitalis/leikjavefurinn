@model IEnumerable<Leikjavefur.Models.GameInstance>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GameID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GameID)
        </td>
    </tr>
}

</table>
